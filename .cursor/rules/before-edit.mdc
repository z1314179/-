---
description: 改文件前读当前工作区；严格按该文件已有格式；不多加；不确定先问
globs: "**/*"
alwaysApply: true
---

# 修改文件前（**强调**）

1. **动手改之前**：用 Read 等工具查看工作区里**当前文件**的格式与结构（模板、缩进、组件写法、已有 class、script 组织方式等）。  
2. **不要**仅凭对话历史或「上次改过的样子」推断文件内容；工作区里的版本才是唯一依据。  
3. **每次修改都按该文件当前已给出的格式改**：在现有风格上只做需求要求的最小改动，**不擅自多加**结构、样式、字段、封装或「顺手」优化。  
4. **不确定**是否该加某段代码、是否该换一种写法或与需求有歧义时，**先询问用户**，不要自行发挥。  
5. **未明确要求时**，不要给组件设置 **`size`**（如 `a-button` 的 `size="small"`、`a-space` 的 `:size`、表格密度等）；沿用文件里已有写法；若需统一尺寸由用户提出后再改。
